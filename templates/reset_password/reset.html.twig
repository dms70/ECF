{% extends 'base.html.twig' %}

{% block title %}Reset your password{% endblock %}

{% block body %}
       <div class="row align-items-center justify-content-center">
       <span class="badge bg-primary-soft"> Date du jour : {{ "now"|date("d-m-Y") }}</span>         
       <span class="badge bg-primary-soft"> Parametre </span>     
      </div>
           

    <!-- CONTENT -->
    <section>
      <div class="container d-flex flex-column">
        <div class="row align-items-center justify-content-center gx-0 min-vh-100">
          <div class="col-12 col-md-6 col-lg-4 py-2 py-md-11">

    <span class="badge bg-primary-soft">   
    <h1>Changer votre mot de passe</h1>
    </span> 
    <br></br>
    {{ form_start(resetForm) }}
        {{ form_row(resetForm.plainPassword) }}
        <button class="btn btn-primary">Validation</button>
    {{ form_end(resetForm) }}

        </div> <!-- / .row -->
      </div> <!-- / .container -->
    </section>
{% endblock %}
